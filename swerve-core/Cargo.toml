[package]
name = "swerve-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
aes-gcm = "0.10"
rand = "0.8"
hex = "0.4"
zeroize = "1"

[dev-dependencies]
serde_json = "1"
